# TECH VANGUARD SWEEP — Fleet Report (ELON + Whetstone)
*2026-06-21 · 18 agents · self-orchestrated, informing Hale*

## ELON — Adoption Shortlist
**INTEGRATE_NOW:** security-guidance (official Anthropic plugin), wshobson/agents
**TRIAL:** Nested subagents (Claude Code), Omnigent (omnigent-ai/omnigent), Agent Teams (Claude Code experimental), Claude Code Routines (cloud scheduled agents), Brand Voice plugin (anthropics/knowledge-work-plugins)
**WATCH:** Marketing plugin (anthropics/knowledge-work-plugins), Ruflo / Claude-Flow (ruvnet/ruflo), Claude Agent SDK (orchestrator-worker) — formalize current stack

ELON — Tech Scout Consolidation, 2026-06-21. Fleet returned ~11 distinct candidates (the 58 raw finds dedup to these; four overlapping orchestration/meta-harness entries collapsed into one category, two knowledge-work plugins share one repo). All repos below verified live via GitHub API today — exists, not archived, license + active push confirmed. Docs-host feature claims (code.claude.com) I could not independently verify past my knowledge cutoff and have labeled docs-stated; I kept anything unverifiable OUT of INTEGRATE_NOW. Zero candidates dropped as hallucination — the fleet's finds were real.

Verdict: two adopt-now, both free and reversible. Everything with a dollar cost, paid-Opus burn, or an unverifiable feature claim was held to TRIAL by our own financial gate, regardless of how the fleet tagged it.

— TOP 5 FOR THE COMMANDER —

1. security-guidance — INTEGRATE NOW. WHAT: Official Anthropic Claude Code plugin (verified in anthropics/claude-plugins-official, the Anthropic-managed directory). Three layers of automatic security review on every edit/turn/commit — catches hardcoded secrets, injection, unsafe patterns before they hit a PR. WHY NOW: We hand Claude write access to code that touches gmail_token.json, portal cookies, client PII, and the 6 protected email/relay files — with ZERO automated guardrail today. This is a free standing net under every edit. COST: $0, all plans. FIRST STEP: /plugin install, run one session editing a NON-protected script, read its findings. If it flags real issues, it earns a permanent slot.

2. wshobson/agents — INTEGRATE NOW. WHAT: MIT plugin marketplace, verified 37,007 stars, active (pushed yesterday) — ~184 prebuilt subagents + orchestrator workflows, install-only-what-you-need. Native to Claude Code AND OpenCode. WHY NOW: We already run CC subagents + OpenCode; this is the lowest-friction upgrade in the sector. We lift reference orchestrator/worker patterns instead of hand-coding each one — frees my scouting time for the parts a competitor can't copy (client voice, dossiers). COST: $0, MIT. FIRST STEP: /plugin marketplace add wshobson/agents, install ONE plugin, test against a non-client task. Fully reversible — uninstall.

3. Nested subagents — TRIAL (fleet tagged integrate-now; I demoted it). WHAT: Built-in CC capability — a lead subagent spawns its own children (depth-capped), visible tree. WHY TRIAL not NOW: It's a zero-cost technique already in the runtime, but the specific cap/feature claim is past my cutoff and I won't promote an unverified claim to adopt-now. The bounded test IS the adoption. WHY IT MATTERS: Lets a lead persona fan out 17-client work recursively under one coordinator — depth of parallelism a manual shop can't match. COST: $0. FIRST STEP: Next big parallel job (dossier-freshness scan across 17 clients), let the lead spawn per-client children, watch the tree panel.

4. Omnigent — TRIAL. WHAT: Databricks-origin, Apache-2.0 meta-harness, verified 4,258 stars + active. Its edge is GOVERNANCE, not swarm: hard $-budget policies (pause an agent at $X), human-approval gates on specific actions, and an egress sandbox that injects secrets the agent never sees. WHY NOW: Maps 1:1 onto three live pains — MISSION-296 billing split (hard spend ceiling vs our soft routing rules), the 6 protected files (enforced sandbox vs prompt+supervisor soft control), and CI razor-sharp doctrine (enforce, don't advise). Medium effort = test, not this-week adopt. COST: $0 (Apache-2.0); model usage only. FIRST STEP: Wrap ONE headless spawn (overnight incubator) with a single per-run cost-budget policy in a sandbox. Reversible, zero change to the rest of the stack.

5. Claude Code Routines — TRIAL. WHAT: Anthropic cloud-scheduled CC agents (docs-stated; couldn't independently verify past cutoff) — cron cadence, output to file/webhook/email, no laptop or live session required. WHY NOW: Our OODA heartbeat, EOD/AM briefs, and keepalives depend on systemd timers on John's box + a live session = single point of failure. Routines moves the brief to Anthropic's cloud — fires with the laptop closed. COST: $0 infra, but consumes plan usage per run — Harlan meters burn vs current free local timers before standardizing. FIRST STEP: Port ONE low-stakes job (AM brief gen → johnloucks3 email) to a Routine, run alongside the existing timer for a week, compare.

HONORABLE MENTION (WATCH): Marketing plugin (free, audit/competitive-intel skills are the near-term value; drafting not publish-ready) · Brand Voice (verified partner-built; could consolidate our 15 feedback_voice_*.md rules into one enforced guideline — but ships its own .mcp.json, watch against lean-MCP doctrine) · Ruflo/Claude-Flow (verified 60,664 stars but built for dev swarms, too heavy for a 1-advisor shop — architectural recon only, do NOT deploy the runtime). NOTE: I stripped vendor marketing numbers ("30-40% fewer security comments," "84.8% SWE-bench") — those are claims, not facts, and don't belong in an adoption decision.

## WHETSTONE — Razor-Sharp Report

**REPLACE_NOW:**
- credential-keepalive (keepalive_supervisor + portal_keepalive + centrav) — SWAP: (1) NOW correct the registry — set active_workaround on credential-keepalive to the MISSION-214 manual Regent ASPXAUTH capture + Centrav human re-auth, with a named burn-down owner; the entry's active_workaround:null is factually false (a standing manual workaround == a failure per the registry's own replacement_policy). (2) BUILD: replace milestone-only manual capture with a persistent-real-browser profile (Camoufox/nodriver headed) — the only pattern research names as surviving Akamai. Probe must assert capture-recency vs the NEXT armed milestone, not just current expiry. Owner: Dembe builds, Whetstone keeps current, Sterling gates. METRIC: zero milestones reached on an expired/uncaptured Regent cookie. Independent of the model-router noise — real and client-affecting.
- core/ai_infra/thunderbird_model_router.py — the entire _call_openrouter_real ops path (DEEPSEEK_OPTIMIZED tier, _GEMINI_FLASH_LITE default, GEMINI_VISION tier, and the _call_gemini/_call_openrouter/_call_grok backward-compat shims) — ROOT CAUSE CORRECTED: this is NOT four bad model IDs to repin. OPENROUTER_API_KEY was RETIRED 2026-06-15 by Commander-approved MISSION-267 (verified: commented-out in live .env). _call_openrouter_real() raises on every call. _call_gemini, _call_openrouter, _call_grok ALL route through it (verified lines 406-430) → the whole _call_* ops path is dead by retirement, so the fabricated IDs (qwen3.6-plus-04-02, gemini-3.1-flash-lite-preview-20260303, gemini-flash-2.5-lite) never reach a provider. It only fails silently because callers fall back to native Claude. SWAP: do NOT repin to a live OpenRouter ID (re-introduces a retired provider). RETIRE the OpenRouter tiers and rewire _call_* to the live direct-Google path (call_gemini_large_context already uses live GEMINI_API_KEY direct — that lane is healthy). METRIC: ops_dispatch_404_rate=0/30-day + required_provider_keys_present_pct=100% daily. OWNER: Sterling. Blast radius: same dead config across ~28 files.
- core/learning/model_safeguards.py — DEEPSEEK_PRIMARY_MODEL / QWEN_PLUS_FREE_MODEL / DEEPSEEK_BULK_MODEL / FREE_OPENROUTER_BULK constants (openrouter/deepseek/deepseek-r1:free, openrouter/qwen/qwen3.6-plus-04-02:free) — DOUBLE-PREFIXED + DEAD + tied to a retired provider. Same MISSION-267 retirement: these reference OpenRouter, which is gone. Also mislabels grok ('grok-4-1-fast' vs router's x-ai/grok-4.3 — two strings, no single source of truth) and self-contradicts (one map calls qwen3.6-plus ACTIVE, another DEPRECATED). SWAP: retire the OpenRouter constants with the tiers above; collapse grok to one canonical ID. METRIC: cross_file_model_id_consistency (distinct IDs per logical model = 1), THRESHOLD 1, OWNER Sterling.

**REFRESH:**
- portal-access (Camoufox binary) — DULL. Wrapper 0.4.11 is latest, but the anti-detection asset is the BROWSER BINARY, and the installed binary is v135.0.1-beta.24 / FF135 (verified ~/.cache/camoufox/version.json) vs upstream v150.0.2-beta.25 / FF150 (2026-05-11) — ~15 Firefox majors / ~14 months stale. A FF135 fingerprint presented to Akamai/Imperva behavioral ML while real Firefox is on FF150 is exactly what a layered trust-score wall flags. REFRESH: re-fetch the current Camoufox binary (v150.0.2-beta.25) — same tool, fresh fingerprint, $0, no new dependency, no proxy/spend decision. Add a 'binary_version' field + max-age check to the registry entry and ci_probe_portal_access.py so binary staleness is tracked, not just wrapper presence (the registry's blind spot — it currency-checks the pip wrapper, not the binary). Owner: Whetstone. Threshold: binary within 2 Camoufox releases of upstream; 30-day reeval.
- headless-dispatch (thunderbird_headless_spawn over claude -p / MAX OAuth) — DULL on currency-of-ASSUMPTION, not a stale package (claude CLI 2.1.185 verified current, wrapper is the right pattern). The external assumption — that headless claude -p / MAX-OAuth dispatch is billed/permitted as today — is in flux per MISSION-296 (P0; Anthropic paused a June-15 Agent-SDK billing change). WEAKEST LINK NAMED: billing specifics NOT confirmed from primary source this session (could not reach Anthropic changelog) — confidence LOW on mechanics, MODERATE a shift is real. REFRESH: (1) finish the MISSION-296 invocation audit so the cost surface is known before any change re-activates; (2) confirm Agent-SDK billing vs the Anthropic changelog (primary) and record it in the registry as a DATED assumption with a re-check date; (3) keep the managed-agents API fallback warm. Owner: Sterling. Re-verify every 14 days while MISSION-296 is open.
- a7_model_audit_gate.py — DULL / ROOT-CAUSE & COMPOUNDING FIX. The gate audits models for COST approval (free/flat-fee/banned) but never validates that a configured model ID RESOLVES against the live catalog. That single gap is WHY all four fabricated IDs shipped — lead the model-router section with this, not five parallel 'fix the ID' items. REFRESH: add one existence check to the gate. METRIC: model_id_existence_pass_rate (configured IDs resolving in live catalog) = 100%, weekly, OWNER Sterling. Per A7 charter, the fix becomes a permanent gate rule so the failure cannot recur — this collapses ~5 symptom findings into one systemic guard.

**HEALTHY:** web-fetch (trafilatura + anansi) — RAZOR_SHARP. trafilatura 2.1.0 is latest, uploaded 2026-06-07 (PyPI), installed at latest; anansi covers the SPA/JS-render lane, trafilatura the clean-extract pass. No deprecation, no abandonment, no successor that beats it on article extraction (Jina/Crawl4AI/Firecrawl correctly scoped as fallback/JS-only, not replacements). Manufacturing a flag here would be theater. Keep Jina AI Reader on WATCH as the zero-setup single-URL fallback; hold trafilatura at the 168h window., tech-adoption (power_harvest + nightly_tech_harvest + incubator) — RAZOR_SHARP. Homegrown scout/harvest; correct staleness axis is whether external fetch/intel sources still resolve, and the efficacy probe already checks freshness + fetcher liveness (replaced the old presence check 2026-06-21). No dead source/API. 14-day reeval is appropriately tight for a scouting tool. ONE registry note: tech-adoption INHERITS web-fetch's health — make the dependency explicit so a downstream web-fetch DULL/RED is diagnosed as a cascade, not in isolation., self-observability (ci_sentinel + self_observability F2T2EA + managed-agent alert bird) — RAZOR_SHARP. Homegrown overwatch; no external version to be stale. Efficacy probe live (replaced presence/import check 2026-06-21), 1h window appropriately tight, F2T2EA kill-chain fully owned (Dembe ISR / Whetstone engage / Sterling gate / Hale accountable). The ci_awareness.jsonl -> OODA Observe + brief fix closes the prior 10-hour blind spot. ONE watch-item: the managed-agent alert/engage path shares the same Anthropic upstream-billing exposure as headless-dispatch (MISSION-296) — lower-stakes (alerting, not primary dispatch) so it stays SHARP, but carry the same dated billing-assumption note so both Anthropic-dependent skills re-check together., call_gemini_large_context direct-Google path (GEMINI_API_KEY live, verified) — HEALTHY. Direct Google AI API ($0 marginal on free-tier allowlist), unaffected by the OpenRouter retirement. This is the live Gemini lane and the correct rewire target for the retired _call_* ops path above.

**Report:**
COMMANDER — WHETSTONE RAZOR-SHARP REPORT (32 findings consolidated)

BOTTOM LINE: 3 REPLACE-NOW, 3 REFRESH, 4 SHARP. The model-router findings look like five separate fires; they are ONE root cause with a guard missing. The OpenRouter half of our model router is dead config left over from YOUR OWN approved MISSION-267 retirement (2026-06-15). I corrected the fleet's recommendation on this — see below.

THE ONE CORRECTION THAT CHANGES THE FIX (surfaced, not buried):
The fleet flagged four 'fabricated model IDs' and recommended swapping each to a live OpenRouter ID. That recommendation is STALE. I verified the live .env: OPENROUTER_API_KEY is commented out, tagged '# RETIRED 2026-06-15 MISSION-267 (OpenRouter removal, Commander-approved).' I then traced the code: _call_gemini, _call_openrouter, and _call_grok (router lines 406-430) ALL funnel through _call_openrouter_real(), which raises on the retired key. So the entire _call_* ops path is dead BY RETIREMENT, not by bad ID — the fabricated IDs never reach a provider, and it only fails silently because callers fall back to native Claude. Repinning to a live OpenRouter ID would re-introduce a provider you ordered removed. CORRECT SWAP: retire the OpenRouter tiers and rewire _call_* to the live direct-Google path. call_gemini_large_context already runs on the live GEMINI_API_KEY direct — that lane is healthy and is the rewire target. This collapses 4-5 'fix the ID' findings into one retirement + one rewire.

THE SPINE (why this shipped, and the permanent guard):
a7_model_audit_gate.py checks models for COST approval but NEVER checks that a configured ID resolves against the live catalog. That single gap is how four invented IDs passed audit. Per my charter — every finding becomes a permanent rule — the compounding fix is ONE metric on the gate: model_id_existence_pass_rate = 100%, weekly, owner Sterling. Build that and this class of defect cannot recur. Lead with the gate, not the symptoms.

WHAT'S ROTTING (REPLACE-NOW):
1. credential-keepalive — registry says active_workaround:null; that is FALSE. Regent ASPXAUTH and Centrav are BOTH human-gated (MISSION-214 manual capture + reCAPTCHA/OTP). A standing manual workaround == a failure by our own policy. Correct the registry NOW; build a persistent-real-browser warm-session to retire the human gate. This is the only REPLACE that is client-affecting and independent of the model noise — do NOT let it get lost in it.
2 & 3. OpenRouter-routed router tiers + model_safeguards constants — dead config from MISSION-267. Retire, don't repin.

WHAT NEEDS A REFRESH (DULL, not dead):
- portal-access: Camoufox WRAPPER is current (0.4.11) but the BINARY is FF135 (Mar 2025) vs upstream FF150 (May 2026). A 14-month-stale fingerprint is exactly what Akamai/Imperva behavioral walls flag. Re-fetch the binary — $0, no proxy decision. Add a binary_version + max-age check to the registry; today it tracks the wrapper and is blind to the binary. THE staleness that matters for an evasion tool.
- headless-dispatch: tool is fine (CLI 2.1.185); the ASSUMPTION is stale — Anthropic Agent-SDK billing is in flux (MISSION-296, P0). WEAKEST LINK NAMED: I could not confirm billing mechanics from the Anthropic changelog this session — LOW confidence on specifics, MODERATE that a shift is real. Finish the M-296 audit, date the assumption in the registry, keep managed-agents fallback warm, re-verify every 14 days.

WHAT'S GENUINELY SHARP (leave alone):
trafilatura/web-fetch (latest, 2 weeks old), tech-adoption, self-observability, and the direct-Google Gemini lane. I added two dependency notes so downstream cascades aren't mis-diagnosed: tech-adoption inherits web-fetch's health; self-observability's alert bird shares the same Anthropic billing exposure as headless-dispatch — re-check those two together.

OWNERS & CADENCE: Sterling owns the model-router retirement + the existence-check gate (model_id_existence_pass_rate 100%/wk). Dembe builds the Regent persistent-profile keepalive; Whetstone keeps it current; Sterling gates. Whetstone owns the Camoufox binary refresh + binary_version registry field. Only items reaching you: none require spend except the still-CLOSED Imperva/clean-IP proxy gate, untouched here.