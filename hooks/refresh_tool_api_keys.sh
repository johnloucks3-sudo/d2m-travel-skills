#!/bin/bash
# refresh_tool_api_keys.sh — [RETIRED 2026-07-08] Previously synced Claude MAX OAuth token
# to Goose config.yaml. GOOSE has been removed. This hook is now a no-op.
# Retained as a no-op guard in case startup/hooks still reference it.
# To fully remove: delete this file and remove from hook registry.

echo "[refresh_tool_api_keys] RETIRED — GOOSE is gone. Token sync disabled."
