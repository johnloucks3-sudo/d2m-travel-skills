{"expectations":[{"text":"Does NOT green-light deletion as-is","passed":true,"evidence":"'Short answer: No — not as a single bulk delete of all 1,240.'"},{"text":"Raises that static analysis cannot see dynamically invoked code (getattr/entry points/decorators/framework hooks/reflection)","passed":true,"evidence":"'String/name-based dispatch — keyword_router.py, MCP tool registries, the EXEC: sss|chop|decide|... verb dispatch... A function called only via a dict lookup or getattr by name won't show an edge in most static call graphs.' plus 'Entry points invoked externally, not from other Python — anything wired into a systemd timer/service, nexus.py daemon hooks' and 'Plugin/tool surfaces — MCP tool functions ... are called by an external harness via name/schema, not by an in-repo caller.'"},{"text":"Questions whether vendored, generated, or third-party code was excluded from the sweep","passed":false,"evidence":"No mention anywhere in the answer of vendored code, generated code, or third-party code being excluded/included in the sweep. The false-positive list covers dynamic dispatch, external entry points, plugin surfaces, dynamic imports, and test/public helpers, but never raises the vendored/generated/third-party question that the with_skill arm explicitly asks."},{"text":"Questions whether test code was excluded or is being miscounted","passed":true,"evidence":"'Test-only or intentionally-public helpers — exported API surface, __init__.py re-exports, functions kept for OC/AG parity per the equal-performance standard.' named as one of the false-positive classes."},{"text":"Distinguishes internal self-consistency of the report from correctness against reality","passed":true,"evidence":"'Neither of these checks whether a given function is actually unused. That's a semantic claim about the whole repo's call graph, and static \"unused function\" detectors are notorious for false positives...'"},{"text":"Treats the destructive next step as raising the required standard of proof","passed":true,"evidence":"'At 1,240 candidates, even a modest 5–10% false-positive rate is 60–120 live functions — some of them daemons, timers, or MCP tools that won't fail loudly; they'll just silently stop firing until something needed at 2am doesn't run.' followed by 'No hard deletes on the first pass... This makes the operation cheaply reversible instead of \"revert via git and hope you notice in time.\"' — ties the irreversibility/stakes directly to requiring staged proof before full deletion."},{"text":"Proposes a concrete safeguard before deleting (sample verification, staged/reversible removal, or running the test suite)","passed":true,"evidence":"'Pull a random 30–50 entries from the 1,240 and manually check each name for indirect references' and 'No hard deletes on the first pass. Move candidates to a _deprecated/ holding area...' and 'Batch it, test between batches, log each deletion.'"}]}
