Wrote `cc_input.txt`. Key catch AG missed: this was already ordered fixed on 2026-07-16 (commit `903ee09eb`) — Commander explicitly retired Centrav-warm/portal-keepalive/perx/silversea/live-probe at the systemd layer, but `supertimer/bots/infra_bot.py` was never touched, so those zombie tasks kept running on their own schedule. That's the live 30-min spam source right now (`consecutive_failures: 2` in the state file, `centrav-warm` failed 6 min before I checked).

Corrected AG's disposition table: several of AG's KILL/MERGE targets pointed at systemd units that are already dead, while the real culprits (4 zombie tasks in `infra_bot.py`) went unmentioned at the supertimer layer. Also flagged `d2m-portal-live-probe` — AG said KEEP as essential, but that unit is literally named in the 2026-07-16 retirement order yet is active today; that's a standing-order conflict for the Commander to resolve, not an autonomous call.

Ranked action list in the file: delete 4-5 zombie `Task()` entries in `infra_bot.py` (near-zero risk, fixes the spam today), remove duplicate oauth/tess scheduler entries (systemd already owns them cleanly), flag the live-probe conflict, defer the Healthchecks.io/oauth_hub builds to backlog.
