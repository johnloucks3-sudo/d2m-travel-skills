Proposal written. **Root cause:** OAuth token staleness in headless spawn lifecycle — the keepalive timer fires every ~600s, but spawns initiated seconds before refresh get a stale token in the subprocess. **Fix:** Tighten keepalive to 120s + add prevalidation in spawn wrapper. Safe to execute autonomously (defensive only, no downside). Hale can apply within 1 hour and test over 48h for zero restarts.
