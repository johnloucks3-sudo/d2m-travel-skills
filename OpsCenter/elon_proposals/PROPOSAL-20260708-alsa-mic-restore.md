Root cause: `alsa-mic-restore.service` hardcodes `amixer -c 1` and fires before the kernel finishes enumerating the HD-Audio card — confirmed by identical `Invalid card number '1'` failures across three separate boots (Jul 07/08 journal), 100% reproduction rate. Systemd target dependencies (what the 07-03 "fix" added) can't fix this — it's a udev-timing issue, not a unit-ordering one. That 07-03 fix was marked "verified" off a manual restart, never a cold boot, which is why the identical failure kept recurring through three more proposals (07-04, 07-05, 07-07) that never checked ground truth against the journal.

Wrote `PROPOSAL-20260708-alsa-mic-restore.md`: recommends disabling the service outright (no confirmed `/voice` usage found anywhere in the live codebase) or, if voice is actually used, a udev-triggered restore instead of target-ordering. Decision: APPLY_AUTONOMOUSLY — infra fix, no Commander gate — and marks the four prior proposals as superseded since none survived an actual cold-boot test.
