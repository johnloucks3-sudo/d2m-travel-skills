Proposal written. Root cause: probe treats network `ECONNRESET` (transient) as auth failure and restarts the gateway; commit `9df1d6d36` added retries but `escalate()` still hardcodes `attempts=1`. Fix is a 3-state classifier (HEALTHY/AUTH_FAILED/TRANSIENT) + service-liveness pre-check + two-strike gate. Decision: **APPLY_AUTONOMOUSLY** — no Commander gate crossed.
